import 'package:flutter/material.dart';

class RefPage extends StatelessWidget {
  const RefPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('REF page'),
      ),
      body: const SizedBox.expand(),
    );
  }
}
